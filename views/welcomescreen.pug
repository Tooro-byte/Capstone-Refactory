doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Young4Chicks Inc
    //-Use Tailwind CSS for modern, responsive styling
    script(src="https://cdn.tailwindcss.com")
    link(rel="preconnect", href="https://fonts.googleapis.com")
    link(rel="preconnect", href="https://fonts.gstatic.com", crossorigin)
    link(href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap", rel="stylesheet")
    style.
      body {
        font-family: 'Inter', sans-serif;
        /* New beautiful radial gradient background for a modern effect */
        background: radial-gradient(circle, #0c1c3c, rgba(2, 24, 1, 1));
        color: white;
      }
      /* Animation for the loading text */
      @keyframes fadeInOut {
        0%, 100% { opacity: 0; }
        50% { opacity: 1; }
      }
      .fade-in-out {
        animation: fadeInOut 2s infinite;
      }
  body.flex.flex-col.items-center.justify-center.min-h-screen.p-4
    //-Logo/Header Section
    .flex.flex-col.items-center.justify-center.mb-6
      //-Wordmark Logo
      h1.text-5xl.sm_text-6xl.font-extrabold.text-white Young4Chicks
    //-Simple Loading Indicator
    .mt-8.text-lg.font-medium.fade-in-out Loading please wait........
    script.
      //-Use a 10-second timeout before redirecting, as requested
      setTimeout(() => {
        window.location.href = '/index';
      }, 10000);
